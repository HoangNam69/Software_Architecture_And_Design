#spring.application.name=cart-service
## Redis Configuration
#spring.data.redis.host=localhost
#spring.data.redis.port=6379
## spring.redis.password=   # N?u Redis c?a b?n c� m?t kh?u
#spring.data.redis.timeout=60000
## C?u h�nh Redis Cache
#spring.cache.type=redis
#spring.cache.redis.time-to-live=3600000
#spring.cache.redis.cache-null-values=false
spring:
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 2000
      lettuce:
        pool:
          max-active: 8 # S? k?t n?i t?i ?a
          max-idle: 8 # S? k?t n?i ide (r?nh r?i) t?i ?a trong pool
          min-idle: 2 # S? k?t n?i ide t?i thi?u lu�n duy tr�
          max-wait: -1ms # Th?i gian ch? t?i ?a khi pool ??y ( -1 = v� h?n)
#      password:
server:
  port: 8086
